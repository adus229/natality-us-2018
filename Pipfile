[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[packages]
pandas = ">=2.2"
numpy = ">=1.26"
matplotlib = ">=3.8"
seaborn = ">=0.13"
pydantic = ">=2.8"
python-dotenv = ">=1.0"
typer = ">=0.12"

[dev-packages]
pytest = ">=8.3"
pytest-cov = ">=5.0"
ruff = ">=0.6"
pre-commit = ">=3.7"
ipykernel = ">=6.29"

[requires]
python_version = "3.10"
